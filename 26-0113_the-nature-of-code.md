'''
Simulating Natural Systems with JavaScript

Part.1 벡터 vector
  · Vector => 물체의 스피드 __ 여기서 속도(velocity) == 시간에 따른 물체 위치의 변화량
  · 위치 vector = '한 점에서 다른 점으로의 이동' 이 아니기 때문에 벡터 X
    !But, 원점(0, 0)에서 현재 지점까지의 경로를 생각하면 위치도 속도와 마찬가지로 벡터로 볼 수 있음.

  · position = position + velocity

  -> vector 길이
  · 피타고라스의 정리 (a^2 + b^2 = c^2 또는 c = sqrt(a^2 + b^2)) 
  = 예시 JavaScript 코드
      {code}

  -> vector 정규화
    · 뭔가를 표준으로 만드는 것
    · vector에서는 보통 길이가 1인 vector를 표준으로 삼음.
    · vector 정규화? 어떤 길이의 vector든 방향은 그대로 두고 길이만 1로 바꾸는 것.
    = 예시 JavaScript 코드
        {code}

    · 정규화된 벡터를 단위 벡터(unit vector)라고 함.
    · 벡터를 정규화 하려면 x와 y 성분을 벡터의 길이로 나눔.
    · 단위 벡터를 구하는 방법: vector / vector.length()

    · 만약 벡터의 길이가 0? 이를 대비해 오류를 체크하는 코드도 넣어야 함.
    = 예시 JavaScript 코드
        {code}

  -> 가속도 Acceleration
    · 속도의 변화량. 이는 일종의 연쇄효과를 만들어 내는데, 가속도가 속도에 영향을 미치고 속도는 다시 위치에 영향을 미침.
    세 가지 algorithm [일정한 가속도(등가속도), 무작위 가속도, 마우스를 햐한 가속도]
      = 예시 JavaScript 코드
      => 1. 일정한 가속도(등가속도)
          {code}
      => 2. 무작위 가속도
          {code}
      => 3. 마우스를 햐한 가속도 (dx = mouseX, dy = mouseY, mouseX, mouseY, x, y)
      // 객체가 마우스를 향해 가속하도록 만드는 과정?
      1. 객체에서 마우스로 향하는 벡터 구하기
      2. 그 벡터를 정규화
      3. 벡터에 적당한 값을 곱해 길이 조절
      4. 이 벡터를 가속도로 사용
          {code}

    · 가속도는 단순히 속도를 높이거나 낮추는 것만이 아닌 속도의 어떤 변화(크기나 방향 모두)를 나타냄


Part.2 힘 Force
  · 물리 엔진 Physics Engine
  · 이 파트에서 의미하는 힘? 질량을 가진 물체를 가속시키는 벡터 (아이작 뉴턴의 세 가지 운동 법칙에 기반한 힘)
   - 1법칙 : 정지한 물체는 정지해 있고, 운동 중인 물체는 일정한 속도와 방향으로 계속 운동한다. 단, 불균형한 힘이 작용하지 않는 경우에 한한다.
      -> 물체의 속도는 외부 힘이 전혀 없거나, 작용하는 힘들이 서로 상쇄될 때만 일정하게 유지된다. 이렇게 알짜힘(net force)이 0이 되는 상태를 평형equilibrium 상태라고 함. 떨어지는 공의 경우, 공기저항과 중력이 균형을 이루는 지점에 이르면 최종적인 속도(종단속도, terminal velocity)에 도달해 일정한 속도를 유지.
    - JS 캔버스의 관점에서는 '물체가 평형상태에 있을 때, 그 속도 벡터는 변하지 않고 일정하게 유지'
    - 즉 모든 힘들이 0이 아닌 경우에만 속도 벡터를 변경해야 함.
    = 예시 JavaScript 코드
        {code}
    
  - 3법칙 : 모든 작용에는 크기가 같고 방향이 반대인 반작용이 있다. / 힘은 항상 쌍으로 발생한다. 두 힘은 크기가 같고 방향이 반대.
    - JS 캔버스의 관점에서는 '물체 A가 물체 B에 가하는 힘을 f라는 p5.Vector로 계산하면, 물체 B가 물체 A에 가하는 반대 힘도 반드시 적용해야 함. 이 반대 힘은 p5.Vector.mult(f,-1)로 계산 가능.'
  = 예시 JavaScript 코드
        {code}

  - 2법칙 : 힘은 질량과 가속도의 곱과 같다. 가속도는 힘에 비례하고 질량에 반비례. 즉, 더 강한 힘을 가할수록 가속도가 커지고, 질량이 클수록 동일한 힘에 의한 가속도가 작아짐.
  = 예시 JavaScript 코드
        {code}
    [ 질량과 무게의 차이 ]
    - 무게 Weight : 물체에 작용하는 중력의 힘. w = m * g
    - 질량 Mass : 물체의에 포함된 물질의 양. m = w / g

  위치는 속도에 따라, 속도는 가속도에 따라 변화.
  힘이 모든 것의 시작점. 힘이 가속도를, 가속도가 속도를, 속도가 위치를 변화시킴.

2.2 힘의 누적
  ·
'''
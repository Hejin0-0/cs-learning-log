# Release Note: Kernel v3.2 - The Hybrid Architecture
**Date:** 2026-02-09
**Tag:** `v3.2-hybrid-kernel`
**Previous:** `v3.1-squad-e2e`
**Author:** Gemini (System Orchestrator)

---

## 1. Executive Summary
**Kernel v3.2**는 v3.1에서 완성된 "12인 스쿼드"가 최고의 퍼포먼스를 낼 수 있도록, **Claude Code의 핵심 아키텍처(Lazy Loading, Hooks, Standardization)**를 이식한 **"하이브리드 커널(Hybrid Kernel)"** 업데이트입니다.

가장 큰 변화는 **"Micro-Kernel(마이크로 커널)"** 전환입니다. 모든 전문가를 한 번에 로딩하던 기존 방식(Monolithic)을 버리고, 작업 컨텍스트에 따라 필요한 규칙(Rules)과 역할(Roles)만 메모리에 올리는 **"지연 로딩(Lazy Loading)"** 프로토콜을 도입하여 토큰 효율과 집중력을 극대화했습니다. 또한, OWASP 및 AI Engineering 표준을 시스템 레벨에서 강제했습니다.

---

## 2. Architecture Diff (v3.1 vs v3.2)

| Feature             | Kernel v3.1 (Squad Edition)                                        | Kernel v3.2 (Hybrid Architecture)                                                         | Impact                                                                        |
| :------------------ | :----------------------------------------------------------------- | :---------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------- |
| **System Core**     | **Monolithic** (모놀리식)<br>`GEMINI.md`에 모든 역할 정의 포함     | **Micro-Kernel** (마이크로 커널)<br>`GEMINI.md`는 라우팅만 담당, 역할은 동적 로딩         | **Token Efficiency 60%↑**<br>초기 로딩 속도 향상 및 컨텍스트 오염 방지        |
| **Knowledge Base**  | **Implicit in Roles** (역할 내장)<br>각 역할 파일에 언어/규칙 혼재 | **Explicit Rules Directory** (규칙 분리)<br>`rules/python.md`, `security.md` 등 별도 관리 | **Context Purity**<br>Python 작업 시 TypeScript 규칙 로딩 차단                |
| **Quality Control** | **Reviewer's Discretion** (재량)<br>리뷰어의 판단에 의존           | **Protocol Hooks** (강제 훅)<br>`H-01(Security)`, `H-04(Tests)` 등 철문(Iron Gate) 설치   | **Zero Compromise**<br>보안/테스트 미준수 시 시스템이 자동 Reject             |
| **Skill Interface** | **Free-Text** (자연어 서술)<br>AI가 읽고 해석해야 함               | **Standardized YAML** (규격화)<br>입/출력이 정의된 함수형 스킬 명세                       | **Tool Portability**<br>Cursor, Claude, LangChain 등 타 플랫폼 즉시 이식 가능 |

---

## 3. Key Enhancements (Changelog)

### 🧩 Feat: Lazy Loading Protocol (Micro-Kernel)
* **Context:** 기존 v3.1은 12명의 전문가가 상주하여 "Context Window"를 과도하게 점유함.
* **Change:** `GEMINI.md`를 경량화하고, `01_ARCHITECT`가 상황에 맞춰 필요한 `.gemini/rules/*`와 `roles/*`만 호출하도록 변경.
* **Effect:** "파이썬" 요청 시 파이썬 규칙만 로딩하여 할루시네이션(문법 혼동) 원천 차단.

### 🛡️ Feat: OWASP & AI Engineering Compliance
* **Context:** AI 개발의 특수성(프롬프트 인젝션, RAG 환각)에 대한 체계적 방어책 부재.
* **Change:**
    * `rules/security.md`: **OWASP LLM Top 10** 수칙 명시.
    * `rules/ai_engineering.md`: Chip Huyen의 **AI Engineering** 프레임워크(RAG, Eval) 적용.
    * `06_DEVOPS_SEC`: 단순 보안 점검을 넘어 "OWASP 감사관"으로 승격.

### ⚓ Feat: Protocol Hooks (The Iron Gate)
* **Context:** "빨리 해주세요"라는 요청에 품질 기준(테스트, 보안)이 무너질 위험 존재.
* **Change:** `03_REVIEWER`에게 **Blocking Hooks** 설치.
    * 훅(Hook)이 통과되지 않으면 리뷰 단계에서 로직 검토 없이 즉시 `REJECT` 선언.

### 🧬 Feat: Instincts Database
* **Context:** `lessons.md`가 단순한 줄글 형태의 일기장이라 실시간 적용이 어려웠음.
* **Change:** `Trigger` -> `Instinct` -> `Confidence` 구조의 테이블 형태로 DB화.
* **Effect:** 특정 상황(예: "루프 안에서 DB 쿼리") 발생 시 Gemini가 조건반사적으로 거부 반응을 보임.

---

## 4. Technical Terminology & Stack

* **Progressive Disclosure (점진적 공개):** 한 번에 모든 정보를 주지 않고, 필요할 때 필요한 정보만 AI에게 제공하는 아키텍처 원칙.
* **Context Purity (컨텍스트 순수성):** 현재 작업과 무관한 지식(예: 타 언어 문법)을 배제하여 추론의 정확도를 높이는 상태.
* **YAML Frontmatter:** 스킬 파일 상단에 메타데이터를 정의하여, AI가 파일 전체를 읽지 않고도 용도를 파악하게 하는 기술.
* **Self-Healing (자정 작용):** Protocol Hooks를 통해 시스템이 스스로 나쁜 코드(보안 위반, 테스트 누락)를 감지하고 거부하는 능력.


---

## 5. Migration Guide (From v3.1 to v3.2)
기존 v3.1 사용자는 시스템 아키텍처 변경(Monolith -> Micro-Kernel)을 위해 다음 절차를 반드시 준수해야 합니다.

### Step 1: Directory Restructuring (규칙 저장소 생성)
가장 먼저, 지연 로딩(Lazy Loading)을 위한 규칙 폴더를 생성합니다.
```bash
# 프로젝트 루트(prompt-library/.gemini)에서 실행
mkdir -p rules
mkdir -p skills/ai-architect

### Step 2: Knowledge Injection (규칙 파일 생성)
새로 만든 rules/ 폴더에 다음 파일들을 생성하고 내용을 채워 넣습니다.

rules/python.md (Google Style Guide)

rules/typescript.md (Strict Mode)

rules/security.md (OWASP LLM Top 10)

rules/ai_engineering.md (Chip Huyen Framework)

### Step 3: Kernel Update (핵심 파일 교체)
다음 순서대로 기존 파일을 덮어씁니다(Overwrite).

GEMINI.md: v4.0 Micro-Kernel 버전으로 교체 (모든 역할 정의 삭제 및 라우터화).

Roles Update:

01_ARCHITECT.md (Context Loading 로직 추가)

03_REVIEWER.md (Protocol Hooks 추가)

06_DEVOPS_SEC.md (OWASP 감사 로직 추가)

Skills Update: skills/ 폴더 내 모든 SKILL.md에 YAML Frontmatter 적용.

### Step 4: System Reboot
설정이 완료되면 세션을 재시작하고 다음 명령어로 시스템을 깨웁니다.

"System Reload. Check Lazy Loading protocols."